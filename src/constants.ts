/**
 * IndexedDB constants shared between service worker and client
 */

export const INDEXEDDB_NAME = 'flows-db';
export const INDEXEDDB_VERSION = 3; // v3: Removed redundant email index from auth_sessions
